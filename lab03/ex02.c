#include <stdio.h>
int main() {
        int len=0;
        width=0;
        depth=0;
        printf('program to find volume:\n');
        printf('length? ');
        scanf("%d",&len);
        printf('depth? ');
        scanf("%d",&depth);
        printf('width? ');
        scanf("%d",&width);
        volume = len * width * depth;
        printf("%d",volume);
         
        return 1;
}//end
